---
# tasks file for collectconfig
- block:
  - name: "collect file [{{ collect_file }}]"
    fetch:
      src: "{{ collect_file }}"
      dest: "collectdata/{{ collect_datetime }}/{{ ansible_hostname }}"

  when: >
    collect_file != None
